1- eureka与zookeeper比较
CPA(C:一致性  P:分区容忍性  A:可用性)
Zookeeper：客户端A提交一个操作，在过半数节点成功之后则返回，剩下的节点并未同步，
需要在读取的时候手动到leader节点执行sync操作，极端情况下在不同的网络分区，会报错，不可用。
eureka: 在网络分区的时候还能提供服务的注册及发现的功能。但是注册中心可能会存在过期的数据，并非强一致性。

2- Peer To Peer架构（对等复制）
每个副本之间可以相互读写备份数据（主从架构会有瓶颈）

3- 注册中心会维护可用服务列表、不可用服务列表，客户端默认每5分钟拉取可用的service列表，拉取完之后做随机化，防止请求不均衡。

4-