server:
  port: 8887
  servlet:
    context-path: /spring-boot-common-function
spring:
  application:
    name: spring-boot-common-function
  profiles:
    active: dev
#-----------------------------------DAO层-mybatis-2/2[开始]-----------------------------------------------
mybatis:
  config-location: classpath:mybatis-config.xml
  mapper-locations: classpath:mapper/*.xml

